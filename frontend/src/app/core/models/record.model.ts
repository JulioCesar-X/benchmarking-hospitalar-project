export interface Record {
  id: number;                // Identificador do registro
  service_activity_indicator_id: number;  // Identificador do indicador
  value: string;                    // Valor do registro
  date: string;                     // Data no formato 'YYYY-MM-DD'
}
